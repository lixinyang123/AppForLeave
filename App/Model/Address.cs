namespace App.Model
{
    // <summary>
    /// 请假去向
    /// </summary>
    public class Address : BaseTamplate
    {
        public Address(string name, string source)
        {
            Name = name;
            Source = source;
        }
    }
}
